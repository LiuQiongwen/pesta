
ALTER TABLE knowledge_chunks ADD COLUMN IF NOT EXISTS universe_id uuid REFERENCES universes(id);
UPDATE knowledge_chunks SET universe_id = (SELECT id FROM universes WHERE user_id = knowledge_chunks.user_id AND is_default = true LIMIT 1) WHERE universe_id IS NULL;
